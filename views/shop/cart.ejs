<%- include('../includes/head.ejs')%>

<body>
  <%- include('../includes/navigation.ejs')%>

  <main>
    <% if (products.length > 0) {%>
    <ul>
      <% for (const product of products) {%>
      <li><%= product.id %> <%= product.qty%> pcs.</li>

      <%} %>
    </ul>

    <%} %>
  </main>

  <%- include('../includes/footer.ejs')%>
</body>
